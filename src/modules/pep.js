const mountPep = (spe, empreendimento, fase, bloco, unidade) => {
    console.log('mount:' + spe, empreendimento, fase, bloco, unidade)
}

const parsePep = (pep) => {
    let pieces = pep.split('.')

    console.log('parse:' + pieces)
}

export const mountPep
export const parsePep